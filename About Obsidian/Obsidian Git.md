Auto create commit and push git. 

I highly suggest that you set up your git environment
```
git init
vim .gitignore  # here, ignore .obsidian/workplace
git add .gitignore
git commit -m 'first commit'
```

Then install the plugin.