Must create with [[create full pip package]] with cookiecutter

bump version 
major, minor, patch

Example
```
bumpversion major --no-commit --no-tag
bumpversion minor --no-commit --no-tag
bumpversion patch --no-commit --no-tag
```